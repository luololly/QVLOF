# Disk-Resident RDO Resources

This directory stores configuration files for the disk-resident RDO control layer.

Directory roles:

- `config/`: dataset-level descriptors
- `params/`: run-level RDO parameters

The files describe the control-plane inputs, target systems, dataset groups, and run parameters used by the RDO stages.
